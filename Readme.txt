
Team:five blue petals
We have uploaded our final submission to our team  GitHub classroom account on time. Since our file exceed 100MB we can't submit by webcms3.
Github link:https://github.com/unsw-cse-comp3900-9900-21T1/capstone-project-3900-h11b-five-blue-petals


Install:
1.PyCharm Community Edition 2020.3.2 x64 or higher (if you have Anaconda this could be downloaded there)
2.python 3.6.5 or higher
3.Windows 10 DirectX 12 or higher 
4.Install packages:
pyJWT package version 2.0.1 or higher:

time pacakge:

random package：

smtplib package：

uuid package:

re package:

db package version 0.1.1 or higher：:

sys package:

datetime package:

socket package：

email package version 4.0.2 or higher：

json package：

hashlib package：

os package：

plot package version 0.6.5 or higher：

matplotlib package version 3.4.1 or higher：

flask_sqlalchemy package version 2.4.4 or higher：

SQLAlchemy package version 1.3.23 or higher:

wikipedia package version 1.4.0 or higher：

Flask package version 1.0.2 or higher：

regex package version 2021.4.4 or higher:

if the user is using pycharm, then these packasespackages could be downloaded easily in pythonin the python intepreterinterpreter.
the python packages(for frontend):
axios package version 0.21.1 or higher
	jquery  package version 3.6 or higher
	layui  package version 2.6.4 or higher
theseThese three packages are all in the static folder, so once the user git clone,       they would automatically be downloaded.


A.Run on cloud service: Following the commands:
ssh root@172.105.254.108
password:comp3900hd
Python3 server.py

B.Run on local server:
python3 server.py

If you want to get a more detailed instructions, please read the user document in the report.
